#include <iostream>
#include <string>
#include <vector>
// #include "spec/testmain"
using namespace std;

int main() {
    vector<int> plan = {};
    for (int i : plan) {
        cout << plan << endl;
    }
    return 0;
}
